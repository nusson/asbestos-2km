// Resets & normalize
meyer-reset()
box-sizing-reset()

// Add fonts
// webfont('FontTitle', 'gt-walsheim-pro/bold', $weight: $fw-bold)
// webfont('FontText', 'gt-walsheim-pro/bold', $weight: $fw-bold)
@import url("https://fonts.googleapis.com/css?family=Bowlby+One+SC|Roboto+Slab+Montserrat:700")

@import '/global'
@import '/utils/helpers'
@import '/utils/wysiwyg'
// @todo: add a /utils/form

// .touchevents,
// .swiper-container
// html
a,
button,
img,
video,
input
  &
  &:active
  &:focus
    outline none
    user-select none

.section
  y-padding(60px)
  +tablet()
    y-padding(40px)
  +mobile()
    y-padding(30px)
  .header + .content
    responsive-prop(margin-top, 60px 30px)
